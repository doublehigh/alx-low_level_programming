#include "main.h"

/**
 * _abs - print absolute value of integer
 * @n: int in the question
 * Return: the abs positive values
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
